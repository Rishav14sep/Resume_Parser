# Resume_Parser
An AI resume parser that converts PDF and DOCX resumes into structured JSON using LLaMA (via Groq), with a Streamlit UI for visualizing skills, experience, projects, and certifications.
